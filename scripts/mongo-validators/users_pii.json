{
  "collMod": "users_pii",
  "validator": {
    "$jsonSchema": {
      "bsonType": "object",
      "required": ["createdAt", "updatedAt"],
      "properties": {
        "orgId": { "bsonType": "string" },
        "firstName": { "bsonType": "string" },
        "lastName": { "bsonType": "string" },
        "dateOfBirth": { "bsonType": "date" },
        "sexAtBirth": {
          "bsonType": "string",
          "enum": ["female", "male", "intersex", "unknown"]
        },
        "genderIdentity": { "bsonType": "string" },
        "ethnicity": { "bsonType": "string" },
        "phoneE164": { "bsonType": "string" },
        "email": { "bsonType": "string" },
        "address": {
          "bsonType": "object",
          "properties": {
            "line1": { "bsonType": "string" },
            "line2": { "bsonType": "string" },
            "city": { "bsonType": "string" },
            "postcode": { "bsonType": "string" },
            "country": { "bsonType": "string" }
          },
          "additionalProperties": true
        },
        "createdAt": { "bsonType": "date" },
        "updatedAt": { "bsonType": "date" }
      },
      "additionalProperties": true
    }
  },
  "validationLevel": "moderate",
  "validationAction": "error",
  "indexes": [
    {
      "key": { "orgId": 1, "updatedAt": -1 }
    },
    {
      "key": { "orgId": 1, "email": 1 },
      "options": {
        "unique": true,
        "partialFilterExpression": {
          "orgId": { "$type": "string" },
          "email": { "$type": "string" }
        }
      }
    },

    {
      "key": { "email": 1 },
      "options": {
        "unique": true,
        "partialFilterExpression": {
          "orgId": { "$exists": false },
          "email": { "$type": "string" }
        }
      }
    },
    {
      "key": { "orgId": 1, "phoneE164": 1 },
      "options": {
        "unique": true,
        "partialFilterExpression": {
          "orgId": { "$type": "string" },
          "phoneE164": { "$type": "string" }
        }
      }
    },
    {
      "key": { "phoneE164": 1 },
      "options": {
        "unique": true,
        "partialFilterExpression": {
          "orgId": { "$exists": false },
          "phoneE164": { "$type": "string" }
        }
      }
    },
    { "key": { "lastName": 1, "firstName": 1, "dateOfBirth": 1 } }
  ]
}
