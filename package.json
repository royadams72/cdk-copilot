{
  "name": "ckd-copilot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "db:apply-validators": "tsx scripts/apply-mongo-validators.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "mobile:reverse": "pnpm --filter ./apps/mobile run android:reverse",
    "mobile:dev": "pnpm --filter ./apps/mobile exec expo start --dev-client",
    "core:build": "pnpm -w --filter @ckd/core run build",
    "api:dev": "pnpm  --filter ./apps/api dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.16.1",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@types/node": "^24.5.0",
    "dotenv": "^17.2.3",
    "tsx": "^4.20.5",
    "turbo": "^2.5.6",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "mongodb": "^6.20.0"
  }
}
